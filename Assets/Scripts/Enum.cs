public enum MaskType
{
    Pride,
    Angry,
    Fear,
}

public enum SelectionType
{
    Observe,
    Pressure,
    Accept,
}

public enum MaskState
{
    Unmasked,
    Reacting,
    Hidden
}
public enum EndingType
{
    True,
    Normal,
}